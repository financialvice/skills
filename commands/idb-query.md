---
description: Query InstantDB data using the cx idb CLI
argument-hint: <question or query description>
---

Use the Task tool with subagent_type="cam-skills:instantdb:idb-query" to answer this:

$ARGUMENTS

Pass the full question/request to the subagent. It has complete CLI docs embedded and will efficiently query the database.
